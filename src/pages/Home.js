import React from 'react';

import UserContainer from "../components/UserContainer";

const Home = () => {
    return (
        <div>
            <UserContainer/>
        </div>
    );
};

export default Home;